# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9

# --- Settings & validation ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# --- LLM providers ---
groq>=0.11.0
openai>=1.40.0          # used as an OpenAI-compatible client for Mistral AI's API
httpx>=0.27.0

# --- Resume file conversion ---
# PDF/PNG/JPG go straight to Mistral's OCR API (httpx call in resume_service.py) -- fast,
# no local document-parsing library needed. DOCX is parsed locally via python-docx.
python-docx>=1.1.0
pypdf>=4.0.0            # lightweight local fallback if Mistral OCR is unavailable/fails on a PDF

# --- Testing ---
pytest>=8.0.0
requests>=2.31.0
